# T3 — Config-UI plugin registration

**Status:** TODO
**Stage:** 1 · **Depends on:** —

## Specs

- Tech design §2.1 "UI/UX (config-UI)" — [tech_design_spec.md](../tech_design_spec.md)
- Requirements REQ-8 (and REQ-1/REQ-7 for which fields appear) —
  [requirements_spec.md](../requirements_spec.md)

## Objective

Register ClickUp as a selectable plugin in the config-UI so the whole setup (connection,
folder selection, scope config) can be done from the web interface. Pure frontend; no
dependency on the Python backend code.

## Files

- **Create** `config-ui/src/plugins/register/clickup/`:
  - `config.tsx` — `IPluginConfig` (`plugin:'clickup'`, `name:'ClickUp'`, icon, `sort`,
    connection fields `name`/token/`proxy`/`rateLimitPerHour`, `dataScope` miller-column
    Spaces→Folders, `scopeConfig` entities `['TICKET','CROSS']` + transformation fields).
  - `transformation.tsx` — Collapse panel with the three regex inputs
    (`issueTypeIncident`, `issueTypeBug`, `issueTypeRequirement`).
  - `assets/icon.svg`
  - `index.ts` — re-exports.
- **Modify** `config-ui/src/plugins/register/index.ts` — register `ClickUpConfig`.
- **Modify** the scope-config form (`scope-config-form/index.tsx`) — wire the ClickUp
  transformation panel via `plugin === 'clickup'`.

## Implementation notes

- Reuse built-in connection field components and the standard miller-column data-scope
  browser — no custom data-scope renderer (tech design §2.1).
- Mirror the existing `asana` / `linear` register packages for the regex transformation
  panel pattern and file layout.
- Field keys (`issueTypeIncident`, etc.) must match the backend scope-config field names
  from T2 so saved values round-trip.

## Definition of Done

- ClickUp appears in the plugin list with name + icon; the config-UI builds/lints
  cleanly.
- Connection form shows name/token/proxy/rate-limit; scope-config form shows the three
  regex inputs.
- No other plugin's registration is broken.

## Development progress

**Status:** DONE (2026-06-25)

### Files created (`config-ui/src/plugins/register/clickup/`)
- `config.tsx` — `ClickUpConfig: IPluginConfig`: `plugin:'clickup'`, `name:'ClickUp'`,
  SVG icon import, `sort: 13.5` (slots after Linear=13 without colliding). Connection
  fields `['name', token, 'proxy', rateLimitPerHour]` (token relabeled "API Token",
  rate-limit default 6000), `initialValues.endpoint = https://api.clickup.com/api/v2`.
  `dataScope: { title:'Folders', millerColumn:{ columnCount:2, firstColumnTitle:'Spaces' } }`.
  `scopeConfig: { entities:['TICKET','CROSS'], transformation:{ issueTypeRequirement,
  issueTypeBug, issueTypeIncident } }`.
- `transformation.tsx` — `ClickUpTransformation` Collapse panel (key `TICKET`) with the
  three regex `Input`s. Mirrors `asana/transformation.tsx` exactly.
- `assets/icon.svg` — ClickUp-style mark (currentColor, 54x54), Apache license header.
- `index.ts` — re-exports config + transformation.

### Files modified
- `config-ui/src/plugins/register/index.ts` — imported `ClickUpConfig` (after
  `ClaudeCodeConfig`) and added it to the `pluginConfigs` array.
- `config-ui/src/plugins/components/scope-config-form/index.tsx` — imported
  `ClickUpTransformation` and added a `plugin === 'clickup'` branch (before the `tapd`
  branch) following the asana wiring pattern.

### Casing decision
Transformation keys use **camelCase** (`issueTypeIncident`, `issueTypeBug`,
`issueTypeRequirement`) matching the existing asana/linear config-ui convention. The
config-ui sends these verbatim in the scope-config payload; the backend (snake_case
`issue_type_*`) is expected to alias/map them per the existing DevLake convention.

### Verification
- `tsc --noEmit` (typecheck): PASS, no errors.
- `eslint` on all created/modified files: PASS, no warnings/errors.
- Full `npm run build` not run (vite build is slow); typecheck + lint scoped run is the
  repo's `lint` script (`tsc && eslint`) and covers the changes.

### Deviations
- `docLink` uses a literal URL (`https://clickup.com/api`) rather than a `DOC_URL.PLUGIN`
  entry, following Linear's pattern, to avoid editing the release files. (`docLink` is a
  required field on `IPluginConfig`.)
- Linear has no `transformation.tsx`; asana was used as the structural template for the
  regex panel (as the task instructed).
