# T5 — Tool-table migration

**Status:** DONE
**Stage:** 2 · **Depends on:** T2

## Specs

- Tech design §1.9 "Migration Scripts" — [tech_design_spec.md](../tech_design_spec.md)

## Objective

Add `clickup/migrations.py` that creates the plugin's **tool tables only**, so a fresh
install provisions the ClickUp schema.

## Files

- **Create** `backend/python/plugins/clickup/clickup/migrations.py`.

## Implementation notes

- One `@migration(<ts>, name="initialize schemas for ClickUp")` calling
  `b.create_tables(...)` for `ClickUpConnection`, `ClickUpScopeConfig`, `ClickUpFolder`,
  `ClickUpTask` (the models from T2).
- **Must not** create domain tables (`issues`, `incidents`, `boards`, `board_issues`,
  `issue_assignees`, `incident_assignees`) — they are owned by Go core migrations.
- Follow the `azuredevops/migrations.py` convention: entities column as `json`,
  `connection_id`, `scope_config_id`; ASF header.

## Definition of Done

- `import clickup.migrations` works and the migration is discoverable by the framework.
- Verified (locally or by inspection against the azuredevops migration) that only the
  four tool tables are created and no domain table is touched.

## Development progress

**Status:** DONE (2026-06-25)

### Files created / changed

- `backend/python/plugins/clickup/clickup/migrations.py` — one
  `@migration(20260625000001, name="initialize schemas for ClickUp")` that
  `b.create_tables(...)` for the four tool models (`ClickUpConnection`,
  `ClickUpScopeConfig`, `ClickUpFolder`, `ClickUpTask`). No domain tables created.
- `backend/python/plugins/clickup/clickup/models.py` — added
  `from clickup.migrations import *` (with the "needed to be able to run migrations"
  comment), mirroring `azuredevops/models.py`, so the migration registers into
  `MIGRATION_SCRIPTS` when the plugin is imported.
- `backend/python/plugins/clickup/tests/migrations_test.py` — 3 tests asserting the
  migration is registered once, creates exactly the four `_tool_clickup_*` tables, and
  creates none of the Go-owned domain tables.

### Verification

- `pytest tests/migrations_test.py` → 3 passed; full suite `pytest tests/` → 16 passed
  (including the 3 pre-existing model tests, confirming the models.py import added no
  regression).
- Tables created: `_tool_clickup_clickupconnections`,
  `_tool_clickup_clickupscopeconfigs`, `_tool_clickup_clickupfolders`,
  `_tool_clickup_clickuptasks`.

### Deviations / notes

- The migration declares the four models **locally inside the migration function** as
  non-`table=True` classes (the frozen-schema-snapshot convention from
  `azuredevops/migrations.py`). They are intentionally NOT imported from
  `clickup/models.py`: (1) it avoids a circular import (`models.py` imports
  `migrations.py`), and (2) re-using the real `table=True` models would clash with
  SQLAlchemy's table registry. The local classes carry only the column schema (no
  `source=` pointers or validators, which are irrelevant to table creation).
- The `ClickUpScopeConfig` `entities` (alias of `domain_types`) and `connection_id`
  columns come from the `ScopeConfig` base class, satisfying the §1.9 "entities column
  as json, connection_id, scope_config_id" convention.
